%h1.title Editing Device

= render 'form', device: @device

.level
  .level-left
  .level-right.buttons
    = link_to @device, class: 'button' do
      = t('abort and show')
    
    = link_to devices_path, class: 'button is-danger is-light' do
      = t('abort')
